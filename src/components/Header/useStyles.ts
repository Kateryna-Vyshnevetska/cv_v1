import { makeStyles } from '@material-ui/styles';

export default makeStyles(() => ({
  myPhoto: {
    display: 'inline-block',
    width: '100%',
    height: '100%'
  },
  description: {
    display: 'flex',
    alignItems: 'center',
    textAlign: 'center',
  }
}))